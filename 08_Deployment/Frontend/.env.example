# ── BirdSense Frontend ── Environment Variables ──
# Copy this file to .env.local and fill in the values.

# REQUIRED: Secret key for signing JWT authentication tokens.
# MUST be changed to a strong, random string in production.
JWT_SECRET=change-me-to-a-strong-random-secret

# URL of the FastAPI inference backend.
# Default: http://127.0.0.1:8000 for local dev
# Docker: http://backend:8000
FASTAPI_URL=http://127.0.0.1:8000
