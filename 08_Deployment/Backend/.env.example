# ── BirdSense Backend ── Environment Variables ──
# Copy this file to .env and fill in the values.

# Path to the trained PyTorch model file (.pth)
MODEL_PATH=../../05_Model/Saved_Models/best_model_v3.pth

# Path to the JSON label mapping file
LABEL_MAPPING_PATH=../../04_Labels/Processed_Labels/label_mapping_v3.json

# Inference device: "cpu" or "cuda"
DEVICE=cpu

# Server port
FASTAPI_PORT=8000

# Comma-separated list of allowed CORS origins
ALLOWED_ORIGINS=http://localhost:3000
