# FastAPI Backend for BirdSense ML Inference
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# Authentication
python-jose==3.3.0
passlib==1.7.4

# Database and validation
sqlalchemy==2.0.25
pydantic==2.7.0
email-validator==2.1.0.post1

# Audio processing
librosa==0.10.2
soundfile==0.12.1
noisereduce==3.0.2
numpy==1.26.4
scipy==1.12.0

# Image processing and models
pillow==10.1.0
timm==1.0.3

# Rate limiting
slowapi==0.1.9

# NOTE: PyTorch, torchvision, torchaudio, and timm are installed separately
# in the Dockerfile using the PyTorch CPU wheel index to avoid conflicts.
