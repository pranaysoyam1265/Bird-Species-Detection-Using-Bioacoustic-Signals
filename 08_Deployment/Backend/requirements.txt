# FastAPI Backend for BirdSense ML Inference
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
# PyTorch and related heavy packages are installed separately in the Dockerfile
# to allow using the PyTorch CPU wheel index without replacing PyPI.
# (Do not add `torch`/`timm` here.)
librosa==0.10.2
soundfile==0.12.1
noisereduce==3.0.2
numpy==1.26.4
pydantic==2.7.0
slowapi==0.1.9
