[build]
  base = "08_Deployment/Frontend"
  command = "pnpm run build"
  publish = "out"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "FASTAPI_PORT,ALLOWED_ORIGINS"
  # Tell pnpm to not fail on frozen-lockfile mismatch during CI installation
  npm_config_frozen_lockfile = "false"
